import React from 'react';
import { Button } from '../Button';

import {
    Container
} from './styles';

const Registers: React.FC = () => {
    return(
        <Container>
            <Button>FETCH</Button>
        </Container>
    )
};

export { Registers };